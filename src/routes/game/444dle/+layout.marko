export interface Input {
  content: Marko.Body;
}

// TEMPORARY, for chasing a Chrome-only paint artifact. ?fix=a..g each turn on
// one candidate workaround so they can be compared without a rebuild. Delete
// this and the block at the end of 444dle.module.css once one of them wins.
<const/fix=new URL($global.request.url).searchParams.get("fix")>

<!doctype html>
<html lang="en" class=fix && `probe-${fix}`>
  <head>
    <meta charset="UTF-8">
    <link rel="icon" type="image/png" sizes="32x32" href="./favicon.png">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Guess three four-letter words at once!">
    <link rel="manifest" href="/game/444dle/manifest.json">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-title" content="444dle">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta
      name="theme-color"
      media="(prefers-color-scheme: light)"
      content="#FEF7F1"
    >
    <meta
      name="theme-color"
      media="(prefers-color-scheme: dark)"
      content="#271C13"
    >
    <link rel="apple-touch-icon" href="/game/444dle/icon-192.png">
    <title>444dle</title>
  </head>
  <body>
    <${input.content}/>
  </body>
</html>

<script>
  if ("serviceWorker" in navigator)
    navigator.serviceWorker.register("/game/444dle/sw.js");
</script>
